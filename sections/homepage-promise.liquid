<style>
  /* ===== Bespoke: Promise Section (Dark Luxury with Gold Accent) ===== */
  .bmx-promise {
    --ink: #f5f5f5;
    --muted: #a0a0a0;
    --paper: #252525;
    --accent: #D4AF37;
    background: #1a1a1a;
    position: relative;
    padding: 80px 0 100px 0;
    border-top: 2px solid rgba(212, 175, 55, 0.15);
  }

  .bmx-promise::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, transparent, #D4AF37 50%, transparent);
    opacity: 0.5;
  }
  
  .bmx-promise .wrap {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 24px;
    text-align: center;
  }
  
  .bmx-promise .container {
    background: var(--paper);
    border: 1px solid rgba(212, 175, 55, 0.2);
    border-radius: 18px;
    padding: 56px 48px;
    box-shadow: 0 10px 40px rgba(0,0,0,0.3);
    position: relative;
    transition: box-shadow .3s ease, border-color .3s ease;
  }

  .bmx-promise .container:hover {
    box-shadow: 0 18px 50px rgba(0,0,0,0.5);
    border-color: rgba(212, 175, 55, 0.4);
  }
  
  .bmx-promise .container::after {
    content: "";
    display: block;
    width: 80px;
    height: 3px;
    background: var(--accent);
    margin: 32px auto 0;
    border-radius: 2px;
    box-shadow: 0 0 10px rgba(212, 175, 55, 0.4);
  }
  
  .bmx-promise h2 {
    margin-bottom: 12px;
    font-family: "Playfair Display", Georgia, serif;
    font-size: 34px;
    color: var(--ink);
  }
  
  .bmx-promise .subtitle {
    margin-bottom: 32px;
    font-size: 13px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent);
    font-weight: 500;
  }
  
  .bmx-promise .text {
    margin: 0 auto;
    max-width: 720px;
    line-height: 1.75;
    font-size: 16px;
    color: var(--muted);
  }
  
  .bmx-promise .text strong {
    color: var(--ink);
    font-weight: 600;
  }

  .bmx-promise .text small {
    opacity: 0.7;
    font-size: 14px;
  }
  
  @media (max-width: 480px) {
    .bmx-promise h2 { font-size: 28px; }
    .bmx-promise .subtitle { font-size: 12px; }
    .bmx-promise .text { font-size: 15px; }
  }
</style>

<section class="bmx-promise" aria-labelledby="bm-promise-title">
  <div class="wrap">
    <div class="container">
      <h2 id="bm-promise-title">
        My Promise to You
      </h2>

      <p class="subtitle">
        Quality, integrity, and care in every commission
      </p>

      <div class="text">
        <p>
          Every project is a commissioned piece, created with refinement, discretion, and personal care. 
          The experience is white-glove from the first message to the final delivery. Communication is clear, timely, 
          and thoughtfully handled so you always know what to expect. The entire process is designed to feel smooth, 
          guided, and private.
        </p>
        <br>
        <p>
          If at any point early in the process the direction doesn't feel right, <strong>I will refund your payment*</strong>, 
          handled simply and without hesitation.
        </p>
        <br>
        <p>
          <small>*See the Refund Policy FAQ below for full details.</small>
        </p>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Promise Section",
  "settings": [],
  "presets": [
    {
      "name": "Promise Section"
    }
  ]
}
{% endschema %}
