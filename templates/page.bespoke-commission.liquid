{% layout none %}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bespoke Melodies - Urgent Gift</title>

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-LF2MB2YSM2"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-LF2MB2YSM2');
    </script>

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=AW-17741514624"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'AW-17741514624');
    </script>

    <!-- Contentsquare -->
    <script src="https://t.contentsquare.net/uxa/42886a0035531.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'charcoal': '#1a1a1a',
                        'charcoal-light': '#2d2d2d',
                        'gold': '#D4AF37',
                        'gold-hover': '#b5952f',
                        'off-white': '#f5f5f5',
                    },
                    fontFamily: {
                        'serif': ['"Playfair Display"', 'serif'],
                        'sans': ['"Inter"', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    {{ 'landing-page.css' | asset_url | stylesheet_tag }}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Inter:wght@300;400;500&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
    <noscript><link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Inter:wght@300;400;500&display=swap" rel="stylesheet"></noscript>
    {{ 'section-song-portal.css' | asset_url | stylesheet_tag }}
    <script>
        // CONFIGURATION
        const config = {
            surveyUrl: "https://tally.so/r/OD1kzg",
            playlist: {
                progressiveFunk: "https://cdn.shopify.com/s/files/1/0977/4900/8660/files/Eine_Kleine_Nachtmusik.mp3?v=1763008066",
                adultContemporary: "https://cdn.shopify.com/s/files/1/0977/4900/8660/files/Just_for_Tonight_Our_Lullaby.mp3?v=1763008066",
                powerBallad: "https://cdn.shopify.com/s/files/1/0977/4900/8660/files/Whisper_to_me_then.mp3?v=1763008066",
                popDance: "https://cdn.shopify.com/s/files/1/0977/4900/8660/files/pop_dance.mp3?v=1763008066",
                cinematicRomance: "https://cdn.shopify.com/s/files/1/0977/4900/8660/files/Run_to_me.mp3?v=1763008066",
                country: "https://cdn.shopify.com/s/files/1/0977/4900/8660/files/long_long_road.mp3?v=1763008066"
            }
        };
    </script>

</head>

<body class="antialiased bg-charcoal text-white font-sans selection:bg-gold selection:text-charcoal">

    <!-- Global GA4 Event Logger -->
    <script>
        // Wrap gtag to log all events to console
        if (typeof gtag !== 'undefined') {
            const originalGtag = gtag;
            window.gtag = function() {
                const args = Array.from(arguments);
                if (args[0] === 'event') {
                    console.log('🎯 GA4 Event:', args[1], args[2] || {});
                }
                return originalGtag.apply(this, args);
            };
        }
    </script>

    <!-- Hero Section -->
    <section id="get-started" class="relative min-h-screen flex items-center justify-center overflow-hidden py-20 px-4">
        <div class="absolute inset-0 z-0">
            <div class="absolute inset-0 bg-gradient-to-b from-charcoal via-charcoal/90 to-charcoal"></div>
        </div>

        <div class="relative z-10 max-w-4xl mx-auto text-center space-y-8">
            <h1 id="lp-headline" class="text-4xl md:text-6xl font-bold leading-tight tracking-tight text-white mb-6">
                You Didn't Forget. <br>
                <span class="text-gold italic">You Were Waiting for Perfection.</span>
            </h1>

            <p id="lp-subhead" class="text-xl md:text-2xl text-gray-300 font-light max-w-3xl mx-auto leading-relaxed">
                A bespoke song for your special occasion. Written by a human composer. Orchestrated by AI technology.
                Delivered in as little as <span class="text-gold font-semibold">6 hours</span>.
            </p>

            <!-- Trust Signal Removed -->

            <div class="pt-6">
    <section id="why-ai" class="py-20 px-4 bg-charcoal-light/30">
        <div class="max-w-6xl mx-auto">
            <div class="text-center mb-16">
                <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">Human Artistry. Unconstrained Production.
                </h2>
                <div class="w-24 h-1 bg-gold mx-auto rounded-full shadow-[0_0_10px_#D4AF37]"></div>
            </div>

            <div class="grid md:grid-cols-2 gap-12 items-center">
                <!-- Column 1: The Human Element -->
                <div class="space-y-6 p-8 rounded-2xl bg-white/5 border border-white/5">
                    <div class="flex items-center gap-4 mb-4">
                        <div class="p-3 bg-gold/10 rounded-full text-gold">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z">
                                </path>
                            </svg>
                        </div>
                        <h3 class="text-2xl font-serif text-white">The Human Element</h3>
                    </div>
                    <p class="text-gray-400 leading-relaxed">
                        I write every lyric and melody personally. A machine cannot understand your partner's inside
                        jokes, the specific ache of a memory, or the story of how you met. That part remains 100% human.
                    </p>
                </div>

                <!-- Column 2: The AI Advantage -->
                <div class="space-y-6 p-8 rounded-2xl bg-white/5 border border-white/5">
                    <div class="flex items-center gap-4 mb-4">
                        <div class="p-3 bg-gold/10 rounded-full text-gold">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                            </svg>
                        </div>
                        <h3 class="text-2xl font-serif text-white">The AI Advantage</h3>
                    </div>
                    <p class="text-gray-400 leading-relaxed">
                        Traditional orchestral production requires booking musicians, engineers, and studio space—a
                        process that takes weeks of coordination. By utilizing advanced AI instrumentation, I can
                        orchestrate your song immediately after writing it. We eliminate the logistical delays of the
                        studio, delivering a full symphonic arrangement on the timeline you actually need.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Sonic Palette (6-Grid Player) -->
    <section id="samples" class="py-20 px-4 relative">
        <div class="max-w-7xl mx-auto">
            <div class="text-center mb-16">
                <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">Select Your Sound</h2>
                <div class="w-24 h-1 bg-gold mx-auto rounded-full shadow-[0_0_10px_#D4AF37]"></div>
            </div>

            <!-- Audio Cards Grid (Scrollable on Mobile) -->
            <div class="flex overflow-x-auto pb-8 gap-6 snap-x snap-mandatory md:grid md:grid-cols-2 lg:grid-cols-3 md:overflow-visible md:pb-0 no-scrollbar">
                
                <!-- Card 1: Pop / Romance -->
                <div class="audio-card genre-pop-romance p-6 rounded-xl relative group cursor-pointer transition-all duration-300 min-w-[85vw] md:min-w-0 snap-center"
                    onclick="playTrack('cinematicRomance', this)">
                    <div class="flex justify-between items-start mb-4">
                        <div class="p-3 bg-white/5 rounded-full text-gold group-hover:bg-gold group-hover:text-charcoal transition-colors">
                            <svg class="w-6 h-6 play-icon" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M8 5v14l11-7z"></path>
                            </svg>
                            <div class="playing-indicator">
                                <div class="flex gap-1 h-6 items-end">
                                    <div class="w-1 bg-gold animate-[bounce_1s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_1.2s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_0.8s_infinite]"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <h3 class="text-xl font-serif text-white mb-1">Pop Romance</h3>
                </div>

                <!-- Card 2: Adult Contemporary -->
                <div class="audio-card genre-adult-contemporary p-6 rounded-xl relative group cursor-pointer transition-all duration-300 min-w-[85vw] md:min-w-0 snap-center"
                    onclick="playTrack('adultContemporary', this)">
                    <div class="flex justify-between items-start mb-4">
                        <div class="p-3 bg-white/5 rounded-full text-gold group-hover:bg-gold group-hover:text-charcoal transition-colors">
                            <svg class="w-6 h-6 play-icon" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M8 5v14l11-7z"></path>
                            </svg>
                            <div class="playing-indicator">
                                <div class="flex gap-1 h-6 items-end">
                                    <div class="w-1 bg-gold animate-[bounce_1s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_1.2s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_0.8s_infinite]"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <h3 class="text-xl font-serif text-white mb-1">Adult Contemporary</h3>
                </div>

                <!-- Card 3: Power Ballad -->
                <div class="audio-card genre-power-ballad p-6 rounded-xl relative group cursor-pointer transition-all duration-300 min-w-[85vw] md:min-w-0 snap-center"
                    onclick="playTrack('powerBallad', this)">
                    <div class="flex justify-between items-start mb-4">
                        <div class="p-3 bg-white/5 rounded-full text-gold group-hover:bg-gold group-hover:text-charcoal transition-colors">
                            <svg class="w-6 h-6 play-icon" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M8 5v14l11-7z"></path>
                            </svg>
                            <div class="playing-indicator">
                                <div class="flex gap-1 h-6 items-end">
                                    <div class="w-1 bg-gold animate-[bounce_1s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_1.2s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_0.8s_infinite]"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <h3 class="text-xl font-serif text-white mb-1">Power Ballad</h3>
                </div>

                <!-- Card 4: Progressive Funk -->
                <div class="audio-card genre-progressive-funk p-6 rounded-xl relative group cursor-pointer transition-all duration-300 min-w-[85vw] md:min-w-0 snap-center"
                    onclick="playTrack('progressiveFunk', this)">
                    <div class="flex justify-between items-start mb-4">
                        <div class="p-3 bg-white/5 rounded-full text-gold group-hover:bg-gold group-hover:text-charcoal transition-colors">
                            <svg class="w-6 h-6 play-icon" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M8 5v14l11-7z"></path>
                            </svg>
                            <div class="playing-indicator">
                                <div class="flex gap-1 h-6 items-end">
                                    <div class="w-1 bg-gold animate-[bounce_1s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_1.2s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_0.8s_infinite]"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <h3 class="text-xl font-serif text-white mb-1">Progressive Funk</h3>
                </div>

                <!-- Card 5: Country -->
                <div class="audio-card genre-country p-6 rounded-xl relative group cursor-pointer transition-all duration-300 min-w-[85vw] md:min-w-0 snap-center"
                    onclick="playTrack('country', this)">
                    <div class="flex justify-between items-start mb-4">
                        <div class="p-3 bg-white/5 rounded-full text-gold group-hover:bg-gold group-hover:text-charcoal transition-colors">
                            <svg class="w-6 h-6 play-icon" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M8 5v14l11-7z"></path>
                            </svg>
                            <div class="playing-indicator">
                                <div class="flex gap-1 h-6 items-end">
                                    <div class="w-1 bg-gold animate-[bounce_1s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_1.2s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_0.8s_infinite]"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <h3 class="text-xl font-serif text-white mb-1">Country</h3>
                </div>

                <!-- Card 6: Cinematic Romance -->
                <div class="audio-card genre-pop-romance p-6 rounded-xl relative group cursor-pointer transition-all duration-300 min-w-[85vw] md:min-w-0 snap-center"
                    onclick="playTrack('popDance', this)">
                    <div class="flex justify-between items-start mb-4">
                        <div class="p-3 bg-white/5 rounded-full text-gold group-hover:bg-gold group-hover:text-charcoal transition-colors">
                            <svg class="w-6 h-6 play-icon" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M8 5v14l11-7z"></path>
                            </svg>
                            <div class="playing-indicator">
                                <div class="flex gap-1 h-6 items-end">
                                    <div class="w-1 bg-gold animate-[bounce_1s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_1.2s_infinite]"></div>
                                    <div class="w-1 bg-gold animate-[bounce_0.8s_infinite]"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <h3 class="text-xl font-serif text-white mb-1">Dance Pop</h3>
                </div>
            </div>
        </div>
    </section>

    <!-- Pricing Tiers -->
    <section id="pricing" class="py-20 px-4 bg-charcoal">
        <div class="max-w-7xl mx-auto">
            <div class="grid lg:grid-cols-3 gap-8 items-center">

                <!-- Standard -->
                <div
                    class="order-2 lg:order-1 bg-white/5 border border-white/10 rounded-2xl p-8 hover:bg-white/10 transition-all duration-300">
                    <h3 class="text-xl font-serif text-white mb-2">Standard</h3>
                    <div class="text-3xl font-bold text-gold mb-4">$249</div>
                    <p class="text-gray-400 text-sm mb-8">1 Week</p>
                    <a href="/cart/51587914727700:1"
                        class="block w-full py-3 border border-white/20 rounded-lg text-white hover:bg-white/10 transition-colors text-center">Select
                        Standard</a>
                </div>

                <!-- Emergency (Featured) -->
                <div id="emergency"
                    class="order-1 lg:order-2 bg-gradient-to-b from-charcoal-light to-charcoal border-2 border-gold rounded-2xl p-8 transform scale-105 shadow-2xl shadow-gold/20 relative glow-gold">
                    <div
                        class="absolute top-0 left-1/2 transform -translate-x-1/2 -translate-y-1/2 bg-gold text-charcoal text-xs font-bold px-4 py-1 rounded-full uppercase tracking-wider shadow-lg">
                        Emergency Allocation</div>
                    <h3 class="text-2xl font-serif text-white mb-2">Emergency</h3>
                    <div class="text-4xl font-bold text-gold mb-4">$599</div>
                    <p class="text-gray-400 text-sm mb-8">24 Hours</p>
                    <a id="emergency-tier-button" href="/cart/51587919610132:1"
                        class="block w-full py-4 bg-gold hover:bg-gold-dark text-charcoal rounded-lg font-bold transition-all transform hover:-translate-y-0.5 text-center shadow-lg glow-gold">Start
                        Emergency Commission</a>
                    <!-- Overnight Warning Container -->
                    <div id="overnight-warning-container"></div>
                </div>

                <!-- Express -->
                <div
                    class="order-3 lg:order-3 bg-white/5 border border-white/10 rounded-2xl p-8 hover:bg-white/10 transition-all duration-300">
                    <h3 class="text-xl font-serif text-white mb-2">Express</h3>
                    <div class="text-3xl font-bold text-gold mb-4">$399</div>
                    <p class="text-gray-400 text-sm mb-8">48 Hours</p>
                    <a href="/cart/51587916366100:1"
                        class="block w-full py-3 border border-white/20 rounded-lg text-white hover:bg-white/10 transition-colors text-center">Select
                        Express</a>
                </div>

            </div>
        </div>
    </section>

    <!-- REMOVED: Creative Brief Form (Tally) - Now using direct checkout -->
    <!-- Users select pricing tier and go directly to cart -->

    <!-- The Bespoke Promise (Checkpoint Guarantee) -->
    <section id="guarantee" class="py-20 px-4 bg-charcoal-light/20 border-t border-white/5">
        <div class="max-w-5xl mx-auto text-center">
            <h2 class="text-3xl md:text-4xl font-serif text-white mb-16">The Bespoke Promise</h2>

            <div class="grid md:grid-cols-3 gap-8 md:gap-12 relative">
                <!-- Connecting Line (Desktop) -->
                <div
                    class="hidden md:block absolute top-8 left-1/6 right-1/6 h-px bg-gradient-to-r from-transparent via-gold/30 to-transparent -z-10">
                </div>

                <!-- Step 1 -->
                <div class="group">
                    <div
                        class="text-4xl md:text-5xl font-bold text-gold mb-4 group-hover:scale-110 transition-transform duration-300">
                        100%</div>
                    <h3 class="text-xl font-serif text-white mb-2">Lyric Approval</h3>
                    <p class="text-gray-500 text-sm max-w-xs mx-auto">Full refund if the lyrics don't move you.</p>
                </div>

                <!-- Step 2 -->
                <div class="group">
                    <div
                        class="text-4xl md:text-5xl font-bold text-gold mb-4 group-hover:scale-110 transition-transform duration-300">
                        50%</div>
                    <h3 class="text-xl font-serif text-white mb-2">Pre-Production</h3>
                    <p class="text-gray-500 text-sm max-w-xs mx-auto">Refund before the final rendering begins.</p>
                </div>

                <!-- Step 3 -->
                <div class="group">
                    <div
                        class="text-4xl md:text-5xl font-bold text-gold mb-4 group-hover:scale-110 transition-transform duration-300">
                        25%</div>
                    <h3 class="text-xl font-serif text-white mb-2">Final Polish</h3>
                    <p class="text-gray-500 text-sm max-w-xs mx-auto">Refund if the final mix isn't perfect.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Mini FAQ Section -->
    <section id="faq" class="py-20 px-4 bg-charcoal-light/20 border-t border-white/5">
        <div class="max-w-4xl mx-auto">
            <div class="text-center mb-12">
                <h2 class="text-3xl md:text-4xl font-serif text-white mb-3">Common Questions</h2>
                <div class="w-20 h-px bg-gradient-to-r from-transparent via-gold to-transparent mx-auto"></div>
            </div>

            <div class="space-y-4">
                <!-- FAQ 1 -->
                <div class="faq-item bg-white/5 border border-white/10 rounded-xl overflow-hidden transition-all duration-300 hover:border-gold/30">
                    <button class="faq-question w-full text-left p-6 flex justify-between items-center group" 
                            onclick="this.parentElement.classList.toggle('open')">
                        <span class="text-lg font-serif text-white group-hover:text-gold transition-colors pr-4">
                            What if I'm not sure how to describe what I want?
                        </span>
                        <svg class="faq-icon w-6 h-6 text-gold flex-shrink-0 transition-transform duration-300" 
                             fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                        </svg>
                    </button>
                    <div class="faq-answer overflow-hidden transition-all duration-300 max-h-0">
                        <div class="p-6 pt-0 text-gray-400 leading-relaxed">
                            You don't need musical language or a fully formed concept — that's what my process is for.
                            A short questionnaire and guided chat help me understand the person, story, and emotion behind the song.
                            From there, I translate your answers into a clear creative direction so you never feel responsible for 'directing' the music.
                        </div>
                    </div>
                </div>

                <!-- FAQ 2 -->
                <div class="faq-item bg-white/5 border border-white/10 rounded-xl overflow-hidden transition-all duration-300 hover:border-gold/30">
                    <button class="faq-question w-full text-left p-6 flex justify-between items-center group" 
                            onclick="this.parentElement.classList.toggle('open')">
                        <span class="text-lg font-serif text-white group-hover:text-gold transition-colors pr-4">
                            How do you make sure the first version is right?
                        </span>
                        <svg class="faq-icon w-6 h-6 text-gold flex-shrink-0 transition-transform duration-300" 
                             fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                        </svg>
                    </button>
                    <div class="faq-answer overflow-hidden transition-all duration-300 max-h-0">
                        <div class="p-6 pt-0 text-gray-400 leading-relaxed">
                            Most of the work happens before I start composing. The survey, our conversation, and any reference points you share 
                            give me a strong sense of tone, pacing, and emotional arc. I treat the first version as a finished piece — not a draft — 
                            so when you hear it, it should feel intentional, polished, and true to what you shared.
                        </div>
                    </div>
                </div>

                <!-- FAQ 3 -->
                <div class="faq-item bg-white/5 border border-white/10 rounded-xl overflow-hidden transition-all duration-300 hover:border-gold/30">
                    <button class="faq-question w-full text-left p-6 flex justify-between items-center group" 
                            onclick="this.parentElement.classList.toggle('open')">
                        <span class="text-lg font-serif text-white group-hover:text-gold transition-colors pr-4">
                            Will the song feel artificial because AI is involved?
                        </span>
                        <svg class="faq-icon w-6 h-6 text-gold flex-shrink-0 transition-transform duration-300" 
                             fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                        </svg>
                    </button>
                    <div class="faq-answer overflow-hidden transition-all duration-300 max-h-0">
                        <div class="p-6 pt-0 text-gray-400 leading-relaxed">
                            AI is simply the instrument used to perform and render the music — not the author. I write the lyrics, 
                            shape the melody, define the emotional tone, and guide how the performance should feel. The technology helps 
                            produce the final delivery, but the meaning, structure, and creative decisions behind the song are fully human. 
                            The finished piece should feel personal, expressive, and emotionally grounded — not artificial or generic.
                        </div>
                    </div>
                </div>

                <!-- FAQ 4 -->
                <div class="faq-item bg-white/5 border border-white/10 rounded-xl overflow-hidden transition-all duration-300 hover:border-gold/30">
                    <button class="faq-question w-full text-left p-6 flex justify-between items-center group" 
                            onclick="this.parentElement.classList.toggle('open')">
                        <span class="text-lg font-serif text-white group-hover:text-gold transition-colors pr-4">
                            What is your refund policy?
                        </span>
                        <svg class="faq-icon w-6 h-6 text-gold flex-shrink-0 transition-transform duration-300" 
                             fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                        </svg>
                    </button>
                    <div class="faq-answer overflow-hidden transition-all duration-300 max-h-0">
                        <div class="p-6 pt-0 text-gray-400 leading-relaxed">
                            <strong class="text-white">• Before final lyric approval:</strong> Full refund — no questions asked.<br>
                            <strong class="text-white">• After lyric approval but before first musical rendering:</strong> 50% refund.<br>
                            <strong class="text-white">• After first musical rendering:</strong> 25% refund.<br>
                            <strong class="text-white">• After second musical edit:</strong> Work is complete and non-refundable.<br><br>
                            These stages protect you without locking you in, and I always encourage early communication so we can address 
                            any misalignment as soon as it appears.
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="py-8 px-4 text-center border-t border-white/5">
        <div class="max-w-4xl mx-auto">
            <div class="flex flex-col md:flex-row items-center justify-center gap-4 md:gap-6 mb-4 text-sm">
                <a href="/policies/privacy-policy" class="text-gray-400 hover:text-gold transition-colors">Privacy Policy</a>
                <span class="hidden md:inline text-gray-600">•</span>
                <a href="/policies/terms-of-service" class="text-gray-400 hover:text-gold transition-colors">Terms of Service</a>
                <span class="hidden md:inline text-gray-600">•</span>
                <a href="/policies/refund-policy" class="text-gray-400 hover:text-gold transition-colors">Refund Policy</a>
            </div>
            <p class="text-gray-600 text-sm">&copy; 2025 Bespoke Melodies. All rights reserved.</p>
        </div>
    </footer>



    <!-- Audio Player Logic -->
    <audio id="audioPlayer" preload="none"></audio>
    <script>
        const audioPlayer = document.getElementById('audioPlayer');
        let currentCard = null;
        let currentTrackInfo = { title: '', genre: '' }; // Track current song for analytics

        function playTrack(genre, cardElement) {
            const trackUrl = config.playlist[genre];

            // If clicking the same card that is currently playing
            if (currentCard === cardElement && !audioPlayer.paused) {
                audioPlayer.pause();
                cardElement.classList.remove('active');
                
                // 🎯 Track pause event
                if (typeof gtag === 'function') {
                    gtag('event', 'audio_pause', {
                        'song_title': currentTrackInfo.title,
                        'genre': currentTrackInfo.genre,
                        'duration_seconds': Math.round(audioPlayer.currentTime),
                        'page_location': window.location.pathname
                    });
                }
                
                currentCard = null;
                return;
            }

            // Reset previous card
            if (currentCard) {
                currentCard.classList.remove('active');
            }

            // Get song info for tracking
            const title = cardElement.querySelector('h4')?.textContent || cardElement.querySelector('h3')?.textContent || 'Unknown';
            const genreText = genre; // Use genre parameter as identifier
            currentTrackInfo = { title, genre: genreText };

            // Play new track
            audioPlayer.src = trackUrl;
            audioPlayer.play();

            // Update UI
            cardElement.classList.add('active');
            currentCard = cardElement;

            // 🎯 Track audio play event in Google Analytics
            if (typeof gtag === 'function') {
                gtag('event', 'audio_play', {
                    'song_title': title,
                    'genre': genreText,
                    'page_location': window.location.pathname
                });
            }
        }

        // Reset UI when audio ends
        audioPlayer.addEventListener('ended', () => {
            if (currentCard) {
                currentCard.classList.remove('active');
                
                // 🎯 Track audio completion
                if (typeof gtag === 'function') {
                    gtag('event', 'audio_complete', {
                        'song_title': currentTrackInfo.title,
                        'genre': currentTrackInfo.genre,
                        'page_location': window.location.pathname
                    });
        
        window.addEventListener('scroll', () => {
            // Show sticky CTA after scrolling past hero section
            if (window.scrollY > 800 && !stickyCTAShown) {
                stickyBtn.classList.remove('hidden');
                stickyCTAShown = true;
            } else if (window.scrollY <= 800 && stickyCTAShown) {
                // Hide if scrolled back up
                stickyBtn.classList.add('hidden');
                stickyCTAShown = false;
            }
        });

        // REMOVED: Tally form event listeners - No longer using Tally form
        /*
        window.addEventListener('message', function(event) {
            // Debug: log all Tally messages
            if (event.data && typeof event.data === 'object') {
                console.log('Tally Event:', event.data);
            }
            
            // Check if message is from Tally (multiple possible formats)
            const eventType = event.data?.event || event.data?.type;
            
            if (eventType && (
                eventType === 'Tally.FormStarted' || 
                eventType === 'Tally.PageView' ||
                eventType === 'Tally.FormPageView' ||
                eventType === 'Tally.FormSubmitted' ||
                eventType.includes('Tally')
            )) {
                console.log('Hiding sticky button due to Tally event:', eventType);
                
                // 🎯 Track form begin event in Google Analytics
                if (eventType === 'Tally.FormStarted' && typeof gtag === 'function') {
                    gtag('event', 'form_begin', {
                        'form_name': 'Creative Brief',
                        'page_location': window.location.pathname
                    });
                }
                
                // Hide sticky button when user starts/interacts with form
                if (stickyBtn) {
                    stickyBtn.classList.add('hidden');
                    stickyCTAShown = false;
                }
            }
        });
        */

        // Exit Intent: Show modal when user tries to leave
        // Defer listener to avoid immediate overhead
        setTimeout(() => {
            let exitIntentShown = false;
            document.addEventListener('mouseleave', (e) => {
                if (e.clientY < 10 && !exitIntentShown) {
                    exitIntentShown = true;
                    const modal = document.getElementById('exit-intent-modal');
                    modal.classList.remove('hidden');
                    modal.classList.add('flex');
                }
            });
        }, 3000);

    </script>
    <!-- REMOVED: Tally Embed & Form Tracking Scripts - Now using direct checkout -->
    <!--
    <script>
        (function(){
            // Tally Embed Script
            var d=document,w="https://tally.so/widgets/embed.js",v=function(){
                if(typeof Tally!=="undefined"){
                    Tally.loadEmbeds();
                } else {
                    d.querySelectorAll("iframe[data-tally-src]:not([src])").forEach(function(e){
                        e.src=e.dataset.tallySrc;
                    });
                }
            };
            if(typeof Tally!=="undefined"){
                v();
            } else if(d.querySelector('script[src="'+w+'"]')==null){
                var s=d.createElement("script");
                s.src=w;
                s.onload=v;
                s.onerror=v;
                d.body.appendChild(s);
            }

            // Track when user ACTUALLY starts the Tally form
            // Requires: Tally GA4 integration enabled in Tally dashboard
            let formBeginFired = false;
            
            window.addEventListener('message', function(event) {
                // Parse data if it's a string (which it is, based on your logs)
                let data = event.data;
                try {
                    if (typeof data === 'string') {
                        data = JSON.parse(data);
                    }
                } catch (e) {
                    // Not valid JSON, ignore
                    return;
                }

                // Tally sends "FormPageView" when user views any page in the form
                // We want to track when they click "Start" (which moves them to Page 2)
                if (data && data.event === 'Tally.FormPageView' && data.payload && data.payload.page) {
                    const pageNum = data.payload.page;
                    
                    // Page 2 = "Start" button clicked -> form_begin
                    if (pageNum === 2) {
                        if (!formBeginFired) {
                            formBeginFired = true;
                            if (typeof gtag === 'function') {
                                gtag('event', 'form_begin', {
                                    form_name: 'Creative Brief',
                                    page_location: window.location.pathname
                                });
                            }
                        }
                    }

                    // Track progress for pages AFTER Page 2 (Start)
                    if (pageNum > 2) {
                        if (typeof gtag === 'function') {
                            gtag('event', 'form_progress', {
                                form_name: 'Creative Brief',
                                page_number: pageNum,
                                page_location: window.location.pathname
                            });
                        }
                    }
                }
            });

            // COMMENTED OUT: No longer using URL parameters to pre-fill form
            // Buttons now redirect directly to cart with variant IDs
            function handleHashChange() {
                var hash = window.location.hash;
                
                // Check if hash targets this section (with or without params)
                if (hash.startsWith('#creative-brief')) {
                    
                    // 1. Handle Scrolling (Browser won't auto-scroll if params exist)
                    var section = document.getElementById('creative-brief');
                    if (section) {
                        // Small timeout to ensure layout is ready
                        setTimeout(function(){
                            section.scrollIntoView({ behavior: 'smooth' });
                        }, 100);
                    }

                    // 2. Handle Tally Pre-filling
                    if (hash.includes('?')) {
                        var queryPart = hash.split('?')[1];
                        var urlParams = new URLSearchParams(queryPart);
                        var tier = urlParams.get('tier');
                        var turnaround = urlParams.get('turnaround');
                        var price = urlParams.get('price');
                        
                        var tallyIframe = document.querySelector('iframe[data-tally-src]');
                        if (tallyIframe && (tier || turnaround || price)) {
                            var currentSrc = tallyIframe.getAttribute('data-tally-src');
                            // Clean base URL from any previous params
                            var baseSrc = currentSrc.split('&tier=')[0].split('&turnaround=')[0].split('&price=')[0];
                            
                            var newSrc = baseSrc;
                            if (tier) newSrc += '&tier=' + encodeURIComponent(tier);
                            if (turnaround) newSrc += '&turnaround=' + encodeURIComponent(turnaround);
                            if (price) newSrc += '&price=' + encodeURIComponent(price);
                            
                            if (currentSrc !== newSrc) {
                                tallyIframe.setAttribute('data-tally-src', newSrc);
                                tallyIframe.src = newSrc;
                            }
                        }
                    }
                }
            }

            // Run on load and hash change
            window.addEventListener('DOMContentLoaded', handleHashChange);
            window.addEventListener('hashchange', handleHashChange);
            // Also run immediately in case DOMContentLoaded already fired
            handleHashChange();
        })();
    </script>
    -->
    </script>

    <!-- Business Hours Warning Script -->
    <script>
        function checkBusinessHours() {
            // Get current time in Eastern Time
            const now = new Date();
            const etTime = new Date(now.toLocaleString("en-US", {timeZone: "America/New_York"}));
            const hour = etTime.getHours();
            
            // Check if between 10PM (22:00) and 6AM (6:00)
            const isOvernight = hour >= 22 || hour < 6;
            
            const warningContainer = document.getElementById('overnight-warning-container');
            
            if (isOvernight && warningContainer) {
                warningContainer.innerHTML = `
                    <div class="bg-yellow-900/20 border-2 border-yellow-500/40 rounded-lg p-4 mt-4 text-sm animate-pulse">
                        <div class="flex items-start gap-3">
                            <span class="text-yellow-400 text-2xl flex-shrink-0">⚠️</span>
                            <div class="text-yellow-100">
                                <strong class="text-yellow-300 text-base">Overnight Order:</strong> 
                                <p class="mt-1">Our studio is currently closed. If you order now, your 6-hour delivery timer will begin at <strong class="text-yellow-300">6:00 AM ET tomorrow morning</strong>.</p>
                            </div>
                        </div>
                    </div>
                `;
            } else if (warningContainer) {
                // Clear warning during business hours
                warningContainer.innerHTML = '';
            }
        }
        
        // Run on page load
        checkBusinessHours();
        
        // Re-check every minute to update if time crosses threshold
        setInterval(checkBusinessHours, 60000);
    </script>

    <script>
        (function() {
            function initDSI() {
                // console.log("DSI: Initializing (Inline)..."); 
                // --- CONFIGURATION ---
                const urlParams = new URLSearchParams(window.location.search);
                const rawIntent = urlParams.get('user_intent');
                
                // console.log("DSI: Raw Intent:", rawIntent);

                // If no intent, do nothing
                if (!rawIntent) return;

                const intent = decodeURIComponent(rawIntent).toLowerCase();
                // console.log("DSI: Parsed Intent:", intent);
                
                const headlineEl = document.getElementById('lp-headline');
                const subheadEl = document.getElementById('lp-subhead');

                // console.log("DSI: Elements found?", { headline: !!headlineEl, subhead: !!subheadEl });

                // Helper function to update text
                function updateContent(h1, h2) {
                    // console.log("DSI: Updating content...", { h1, h2 });
                    if (headlineEl) headlineEl.innerHTML = h1;
                    if (subheadEl) subheadEl.innerHTML = h2;
                }

                // --- LOGIC HIERARCHY ---

                // Priority 1: URGENCY
                const urgencyKeywords = ["rush", "minute", "urgent", "fast", "overnight", "emergency", "tomorrow", "tonight", "forgot"];
                if (urgencyKeywords.some(keyword => intent.includes(keyword))) {
                    if (intent.includes("anniversary")) {
                        updateContent(
                            "You Didn't Forget. <br><span class='text-gold italic'>You Were Waiting for a Masterpiece.</span>",
                            "The ultimate anniversary rescue. Composed, recorded, and delivered tonight."
                        );
                        if (typeof gtag === 'function') {
                            gtag('event', 'headline_view', {
                                headline_category: 'Urgency - Anniversary',
                                user_intent_raw: rawIntent,
                                page_location: window.location.pathname
                            });
                        }
                    } else {
                        updateContent(
                            "The Impossible Gift. <br><span class='text-gold italic'>Delivered Tonight.</span>",
                            "Don't just fill her hands with flowers. Fill her heart with a symphony. In your inbox by 8 PM."
                        );
                        if (typeof gtag === 'function') {
                            gtag('event', 'headline_view', {
                                headline_category: 'Urgency - General',
                                user_intent_raw: rawIntent,
                                page_location: window.location.pathname
                            });
                        }
                    }
                    return;
                }

                // Priority 2: APOLOGY
                const apologyKeywords = ["sorry", "apology", "doghouse"];
                if (apologyKeywords.some(keyword => intent.includes(keyword))) {
                    const category = intent.includes("wife") ? 'Apology - Wife' : 'Apology - General';
                    if (intent.includes("wife")) {
                        updateContent(
                            "When \"Sorry\" Isn't Enough. <br><span class='text-gold italic'>Give Her a Symphony.</span>",
                            "Words failed you. Music won't. The only apology grand enough to bridge the silence."
                        );
                    } else {
                        updateContent(
                            "Don't Just Apologize. <br><span class='text-gold italic'>Sweep Her Off Her Feet.</span>",
                            "Turn a mistake into a memory. The ultimate romantic gesture, delivered tonight."
                        );
                    }
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {
                            headline_category: category,
                            user_intent_raw: rawIntent,
                            page_location: window.location.pathname
                        });
                    }
                    return;
                }

                // Priority 3: COMPETITOR COMPARISON
                if (intent.includes("songfinch")) {
                    updateContent(
                        "Don't Buy a Jingle. <br><span class='text-gold italic'>Commission a Legacy.</span>",
                        "Novelty gifts are for laughs. Broadcast-quality music is for love."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {
                            headline_category: 'Competitor - Songfinch',
                            user_intent_raw: rawIntent,
                            page_location: window.location.pathname
                        });
                    }
                    return;
                }

                // Priority 4: STATUS & SPECIFICS
                if (intent.includes("parent") || intent.includes("parents")) {
                    updateContent(
                        "Honor Their Legacy. <br><span class='text-gold italic'>In Song.</span>",
                        "They have a lifetime of \"things.\" Give them a moment of pure emotion."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Parents', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                if (intent.includes("birthday")) {
                    updateContent(
                        "The Birthday Gift She'll Remember <br><span class='text-gold italic'>When She's 80.</span>",
                        "Perfume runs dry. Flowers wilt. A custom song is forever."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Birthday', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                const luxuryKeywords = ["luxury", "everything", "nothing", "sentimental"];
                if (luxuryKeywords.some(keyword => intent.includes(keyword))) {
                    updateContent(
                        "She Has Everything. <br><span class='text-gold italic'>Except a Song Written Just For Her.</span>",
                        "Bespoke art, commissioned for her story. The only luxury that doesn't collect dust."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Luxury', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                // Priority 5: MILESTONES
                if (intent.includes("1st") || intent.includes("1 year")) {
                    updateContent(
                        "One Year Down. <br><span class='text-gold italic'>A Lifetime to Go.</span>",
                        "Paper is traditional. This is eternal. A custom song for your first anniversary."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Milestone - 1st', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                if (intent.includes("5 year") || intent.includes("5th")) {
                    updateContent(
                        "Five Years. Countless Memories. <br><span class='text-gold italic'>One Song.</span>",
                        "Tradition says 'Wood.' Love demands a Masterpiece."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Milestone - 5th', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                if (intent.includes("10 year") || intent.includes("10th")) {
                    updateContent(
                        "A Decade of Love. <br><span class='text-gold italic'>One Defining Anthem.</span>",
                        "Ten years deserves more than tin. It deserves a legacy."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Milestone - 10th', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                if (intent.includes("25 year") || intent.includes("25th")) {
                    updateContent(
                        "A Quarter Century. <br><span class='text-gold italic'>Immortalized.</span>",
                        "Diamonds sit in a drawer. This melody lives in the air between you."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Milestone - 25th', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                if (intent.includes("50 year") || intent.includes("50th")) {
                    updateContent(
                        "Golden Era. <br><span class='text-gold italic'>Immortalized.</span>",
                        "Diamonds sit in a drawer. This melody lives in the air between you."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Milestone - 50th', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                const milestoneRegex = /(\d+)\s*(year|th|nd|rd|st)/;
                if (milestoneRegex.test(intent)) {
                     updateContent(
                        "A Lifetime of Love. <br><span class='text-gold italic'>Captured in Song.</span>",
                        "Tradition suggests a gift. Love demands a Masterpiece."
                    );
                    if (typeof gtag === 'function') {
                        gtag('event', 'headline_view', {headline_category: 'Milestone - Other', user_intent_raw: rawIntent, page_location: window.location.pathname});
                    }
                    return;
                }

                // Priority 6: DEFAULT
                updateContent(
                    "Your Love Story. <br><span class='text-gold italic'>In Song.</span>",
                    "The intimacy of a love letter. The emotional scale of a movie soundtrack. Composed exclusively for her."
                );
                if (typeof gtag === 'function') {
                    gtag('event', 'headline_view', {headline_category: 'Default', user_intent_raw: rawIntent, page_location: window.location.pathname});
                }
            }

            if (document.readyState === "loading") {
                document.addEventListener("DOMContentLoaded", initDSI);
            } else {
                initDSI();
            }
        })();
    </script>

    <!-- Scroll Depth Tracking -->
    <script>
        (function() {
            const scrollDepths = {25: false, 50: false, 75: false, 100: false};
            
            function checkScrollDepth() {
                const scrollPercent = Math.round((window.scrollY + window.innerHeight) / document.documentElement.scrollHeight * 100);
                
                for (let depth in scrollDepths) {
                    if (!scrollDepths[depth] && scrollPercent >= parseInt(depth)) {
                        scrollDepths[depth] = true;
                        console.log('🎯 GA4 Event: scroll', {percent_scrolled: parseInt(depth), page_location: window.location.pathname});
                        if (typeof gtag === 'function') {
                            gtag('event', 'scroll', {
                                percent_scrolled: parseInt(depth),
                                page_location: window.location.pathname
                            });
                        }
                    }
                }
            }
            
            let scrollTimeout;
            window.addEventListener('scroll', function() {
                clearTimeout(scrollTimeout);
                scrollTimeout = setTimeout(checkScrollDepth, 100);
            }, {passive: true});
        })();
    </script>

    <!-- FAQ Click Tracking -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const faqButtons = document.querySelectorAll('.faq-question');
            faqButtons.forEach(function(button) {
                button.addEventListener('click', function() {
                    const questionText = this.querySelector('span').textContent.trim();
                    if (typeof gtag === 'function') {
                        gtag('event', 'faq_click', {
                            question: questionText.substring(0, 100),
                            page_location: window.location.pathname
                        });
                    }
                });
            });
        });
    </script>
</body>

</html>
